---
phase: 05-testing-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/account/domain/exceptions/duplicate-auth0-sub.error.spec.ts
  - src/shared/infrastructure/config/env.validation.ts
  - src/app.module.ts
  - src/main.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]

must_haves:
  truths:
    - "DuplicateAuth0SubError has unit test coverage proving code, message, and metadata"
    - "Application fails fast at boot if required env vars (DATABASE_URL, AUTH0_DOMAIN, AUTH0_AUDIENCE, S3_*) are missing"
    - "Application shuts down gracefully on SIGTERM/SIGINT, running onModuleDestroy hooks"
    - "All existing unit tests (119+) continue to pass"
  artifacts:
    - path: "src/account/domain/exceptions/duplicate-auth0-sub.error.spec.ts"
      provides: "Unit test for DuplicateAuth0SubError"
      min_lines: 15
    - path: "src/shared/infrastructure/config/env.validation.ts"
      provides: "Joi validation schema for required env vars"
      exports: ["envValidationSchema"]
    - path: "src/app.module.ts"
      provides: "ConfigModule.forRoot with validationSchema"
      contains: "envValidationSchema"
    - path: "src/main.ts"
      provides: "enableShutdownHooks call"
      contains: "enableShutdownHooks"
  key_links:
    - from: "src/app.module.ts"
      to: "src/shared/infrastructure/config/env.validation.ts"
      via: "import envValidationSchema"
      pattern: "import.*envValidationSchema"
    - from: "src/app.module.ts"
      to: "ConfigModule.forRoot"
      via: "validationSchema option"
      pattern: "validationSchema.*envValidationSchema"
---

<objective>
Install hardening dependencies, close the last test coverage gap (DuplicateAuth0SubError), add env var validation at boot, and enable graceful shutdown.

Purpose: TEST-01 through TEST-06 are already covered by the 119 unit tests and 37 E2E tests built in prior phases. This plan closes the single remaining unit test gap (DuplicateAuth0SubError) and adds two operational hardening features (config validation, graceful shutdown) that require minimal code changes.

Output: DuplicateAuth0SubError spec file, Joi env validation schema wired into AppModule, graceful shutdown enabled in main.ts, @nestjs/terminus and joi installed for this and next plan.
</objective>

<execution_context>
@C:/Users/felip/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/felip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-testing-and-hardening/05-CONTEXT.md
@.planning/phases/05-testing-and-hardening/05-RESEARCH.md

@src/account/domain/exceptions/duplicate-auth0-sub.error.ts
@src/shared/domain/exceptions/domain-exception.base.ts
@src/app.module.ts
@src/main.ts
@src/setup-app.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/account/domain/exceptions/duplicate-auth0-sub.error.ts:
```typescript
import { DomainException } from '../../../shared/domain/exceptions/domain-exception.base';

export class DuplicateAuth0SubError extends DomainException {
  public readonly code = 'DUPLICATE_AUTH0_SUB';
  constructor(auth0Sub: string) {
    super(`Auth0 subject already linked: ${auth0Sub}`, { auth0Sub });
  }
}
```

From src/app.module.ts:
```typescript
@Module({
  imports: [
    ConfigModule.forRoot(),
    EventEmitterModule.forRoot(),
    PrismaModule,
    AuthModule,
    AccountInterfaceModule,
  ],
})
export class AppModule {}
```

From src/main.ts:
```typescript
async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  setupApp(app);
  await app.listen(process.env.PORT ?? 3000);
}
void bootstrap();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install deps and add DuplicateAuth0SubError unit test</name>
  <files>package.json, src/account/domain/exceptions/duplicate-auth0-sub.error.spec.ts</files>
  <behavior>
    - Test 1: DuplicateAuth0SubError extends DomainException
    - Test 2: error.code equals 'DUPLICATE_AUTH0_SUB'
    - Test 3: error.message contains the auth0Sub string passed to constructor
    - Test 4: error.metadata equals { auth0Sub: 'auth0|abc123' }
  </behavior>
  <action>
    1. Run `npm install @nestjs/terminus joi` to install dependencies needed by this plan and the next plan (health check).

    2. Create `src/account/domain/exceptions/duplicate-auth0-sub.error.spec.ts` following the exact test pattern from RESEARCH.md:
       - Import DuplicateAuth0SubError and DomainException
       - 4 test cases: instanceof check, code property, message content, metadata content
       - Use 'auth0|abc123' as the test auth0Sub value

    This is a simple class test -- the error is a defensive guard (per user decision). Test the class itself, do NOT try to find a use case that throws it.
  </action>
  <verify>
    <automated>npx vitest run src/account/domain/exceptions/duplicate-auth0-sub.error.spec.ts --reporter=verbose</automated>
  </verify>
  <done>4 tests pass covering DuplicateAuth0SubError construction, code, message, and metadata. @nestjs/terminus and joi are installed in package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Add env validation schema and graceful shutdown</name>
  <files>src/shared/infrastructure/config/env.validation.ts, src/app.module.ts, src/main.ts</files>
  <action>
    1. Create `src/shared/infrastructure/config/env.validation.ts`:
       - Use `import * as Joi from 'joi'` (namespace import -- NOT default import, Joi uses CommonJS exports)
       - Export `envValidationSchema` as a Joi.object with these required fields:
         - DATABASE_URL: Joi.string().required()
         - AUTH0_DOMAIN: Joi.string().required()
         - AUTH0_AUDIENCE: Joi.string().required()
         - S3_ENDPOINT: Joi.string().required()
         - S3_BUCKET: Joi.string().required()
         - S3_REGION: Joi.string().required()
         - S3_ACCESS_KEY: Joi.string().required()
         - S3_SECRET_KEY: Joi.string().required()
         - PORT: Joi.number().default(3000)

    2. Update `src/app.module.ts`:
       - Import envValidationSchema from './shared/infrastructure/config/env.validation.js'
       - Change `ConfigModule.forRoot()` to `ConfigModule.forRoot({ validationSchema: envValidationSchema, validationOptions: { abortEarly: false } })`
       - Keep all other imports unchanged

    3. Update `src/main.ts`:
       - Add `app.enableShutdownHooks()` AFTER `setupApp(app)` and BEFORE `app.listen()`
       - Do NOT add enableShutdownHooks in setupApp.ts (only main.ts) -- tests use app.close() explicitly, and enableShutdownHooks adds event listeners that cause MaxListenersExceededWarning in tests
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>env.validation.ts exports Joi schema with all required env vars. AppModule uses validationSchema in ConfigModule.forRoot. main.ts calls enableShutdownHooks. All existing unit tests still pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` -- all unit tests pass (119+ existing + 4 new DuplicateAuth0SubError tests)
2. `grep -r "envValidationSchema" src/app.module.ts` -- confirms wiring
3. `grep "enableShutdownHooks" src/main.ts` -- confirms graceful shutdown
4. `grep "terminus\|joi" package.json` -- confirms deps installed
</verification>

<success_criteria>
- DuplicateAuth0SubError has 4 passing unit tests
- Joi env validation schema created and wired into AppModule
- Graceful shutdown enabled in main.ts
- @nestjs/terminus and joi installed
- All 119+ existing unit tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-and-hardening/05-01-SUMMARY.md`
</output>
