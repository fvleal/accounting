---
phase: 02-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/OnboardingPage.tsx
  - src/hooks/useCreateAccount.ts
  - src/__tests__/OnboardingPage.test.tsx
autonomous: true
requirements: [AUTH-04]

must_haves:
  truths:
    - "User can submit full name and CPF on the onboarding form and is redirected to the profile page on success"
    - "Submitting an invalid CPF shows an inline validation error without navigating away"
    - "Submitting an invalid name (single word or short parts) shows an inline validation error"
    - "Submit button is disabled with spinner while request is in flight"
    - "Backend errors (e.g., CPF already exists) show inline error under relevant field"
    - "React Query cache is seeded with POST response so profile page loads instantly"
  artifacts:
    - path: "src/pages/OnboardingPage.tsx"
      provides: "Full onboarding form with name + CPF fields, validation, submission"
      min_lines: 80
    - path: "src/hooks/useCreateAccount.ts"
      provides: "React Query mutation for POST /accounts with cache seeding"
      exports: ["useCreateAccount"]
    - path: "src/__tests__/OnboardingPage.test.tsx"
      provides: "Tests for form validation, submission, error handling"
  key_links:
    - from: "src/pages/OnboardingPage.tsx"
      to: "src/hooks/useCreateAccount.ts"
      via: "useCreateAccount() hook call"
      pattern: "useCreateAccount\\(\\)"
    - from: "src/hooks/useCreateAccount.ts"
      to: "src/api/accounts.ts"
      via: "mutationFn calling createAccount"
      pattern: "createAccount"
    - from: "src/hooks/useCreateAccount.ts"
      to: "queryClient cache"
      via: "setQueryData on success"
      pattern: "setQueryData.*account.*me"
    - from: "src/pages/OnboardingPage.tsx"
      to: "react-hook-form"
      via: "useForm + Controller for MUI fields"
      pattern: "useForm|Controller"
    - from: "src/pages/OnboardingPage.tsx"
      to: "src/utils/cpf.ts"
      via: "maskCpf in onChange, unmaskCpf before submit"
      pattern: "maskCpf|unmaskCpf"
---

<objective>
Build the onboarding form where new users create their Account by submitting full name and CPF.

Purpose: This is the account creation flow that completes the onboarding experience. After submitting, the user is redirected to the profile page with their account data already cached in React Query.

Output: Full OnboardingPage with react-hook-form, CPF masking/validation, useCreateAccount mutation with cache seeding, and comprehensive tests.
</objective>

<execution_context>
@C:/Users/felip/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/felip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding/02-CONTEXT.md
@.planning/phases/02-onboarding/02-RESEARCH.md
@.planning/phases/02-onboarding/02-VALIDATION.md
@.planning/phases/02-onboarding/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (created by 02-01) -->

From src/api/accounts.ts:
```typescript
import apiClient from './client';
import type { Account } from '../types/account';

export function getMe(): Promise<Account>;
export function createAccount(data: { name: string; cpf: string }): Promise<Account>;
```

From src/utils/cpf.ts:
```typescript
export function maskCpf(value: string): string;
export function unmaskCpf(value: string): string;
```

From src/hooks/useAccount.ts:
```typescript
export function useAccount(): UseQueryResult<Account>;
// queryKey: ['account', 'me']
```

From src/types/account.ts:
```typescript
export interface Account {
  id: string;
  fullName: string;
  email: string;
  cpf: string;
  dateOfBirth: string | null;
  phone: string | null;
  photoUrl: string | null;
  createdAt: string;
  updatedAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useCreateAccount mutation hook and OnboardingPage tests</name>
  <files>src/hooks/useCreateAccount.ts, src/__tests__/OnboardingPage.test.tsx</files>
  <behavior>
    - Onboarding form renders name and CPF fields
    - Submitting empty form shows required-field errors for both fields
    - Submitting single-word name shows "Informe nome e sobrenome" error
    - Submitting name with 1-char word shows "Cada parte do nome deve ter pelo menos 2 caracteres" error
    - Submitting invalid CPF (e.g., "111.111.111-11") shows "CPF invalido" error
    - CPF field auto-masks as user types (XXX.XXX.XXX-XX)
    - Successful submission calls POST /accounts with { name, cpf } (cpf unmasked) and redirects to "/"
    - Successful submission shows "Conta criada!" toast
    - Backend 409 error with CPF message shows inline error on CPF field
    - Submit button shows loading state during submission
  </behavior>
  <action>
    1. Create `src/hooks/useCreateAccount.ts`:
       - Import `useMutation, useQueryClient` from `@tanstack/react-query`
       - Import `createAccount` from `../api/accounts`
       - Import `Account` from `../types/account`
       - `useCreateAccount()` returns `useMutation` with:
         - `mutationFn: (data: { name: string; cpf: string }) => createAccount(data)`
         - `onSuccess: (account) => queryClient.setQueryData(['account', 'me'], account)`
       - CRITICAL: Use exact query key `['account', 'me']` matching useAccount hook (Pitfall #3)

    2. Create `src/__tests__/OnboardingPage.test.tsx`:
       - Set up test wrapper with QueryClientProvider (fresh client per test) and MemoryRouter and SnackbarProvider
       - Mock `../hooks/useCreateAccount` to control mutation behavior
       - Mock `react-router` useNavigate
       - Mock `notistack` useSnackbar
       - Write RED tests for all behaviors listed above:
         a. "renders name and CPF fields" -- find by label
         b. "shows required errors on empty submit" -- submit form, check helperText
         c. "validates name requires two words" -- type "Fulano", blur, check error
         d. "validates name word length" -- type "Jo A", blur, check error
         e. "validates CPF check digits" -- type "111.111.111-11", blur, check error
         f. "masks CPF input" -- type "12345678909", verify field shows "123.456.789-09"
         g. "submits and redirects on success" -- fill valid data, submit, verify mutate called with { name: "Fulano Silva", cpf: "12345678909" }, verify navigate("/")
         h. "shows toast on success" -- verify enqueueSnackbar called with "Conta criada!"
         i. "shows inline CPF error on backend 409" -- simulate mutation onError with 409 response
         j. "shows loading state on submit button" -- verify button has loading prop during submission
       - Run tests -- should FAIL (OnboardingPage not yet implemented)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/OnboardingPage.test.tsx --reporter=verbose 2>&1 | head -50</automated>
  </verify>
  <done>useCreateAccount hook exists with cache seeding. OnboardingPage tests written and failing RED (expected -- implementation comes in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Implement full OnboardingPage with form, validation, and submission</name>
  <files>src/pages/OnboardingPage.tsx</files>
  <action>
    Replace the placeholder OnboardingPage from Plan 01 with the full implementation.

    1. **Imports:**
       - `useForm, Controller` from `react-hook-form`
       - `cpf` from `cpf-cnpj-validator`
       - `maskCpf, unmaskCpf` from `../utils/cpf`
       - `useCreateAccount` from `../hooks/useCreateAccount`
       - `useNavigate` from `react-router`
       - `useSnackbar` from `notistack`
       - MUI: `Container, Card, CardContent, Typography, TextField, Button, Box`

    2. **Form setup:**
       - `interface OnboardingFormData { name: string; cpf: string; }`
       - `useForm<OnboardingFormData>({ defaultValues: { name: '', cpf: '' }, mode: 'onBlur' })`
       - mode: 'onBlur' per user decision (validation triggers on blur)

    3. **Name field (Controller):**
       - label: "Nome completo"
       - Validation rules:
         - required: "Nome completo e obrigatorio"
         - validate: split on whitespace, require 2+ words each 2+ chars
         - Error messages: "Informe nome e sobrenome" (< 2 words), "Cada parte do nome deve ter pelo menos 2 caracteres" (short words)
       - Follow exact nameRules pattern from RESEARCH.md

    4. **CPF field (Controller):**
       - label: "CPF"
       - placeholder: "000.000.000-00"
       - onChange: wrap with `maskCpf(e.target.value)` for auto-masking
       - Validation rules:
         - required: "CPF e obrigatorio"
         - validate: `cpf.isValid(unmaskCpf(value)) || 'CPF invalido'`
       - Show error/helperText from fieldState

    5. **Submit handler:**
       - `handleSubmit((data) => mutation.mutate({ name: data.name.trim(), cpf: unmaskCpf(data.cpf) }))`
       - Strip mask from CPF before sending to backend
       - Trim name whitespace

    6. **Mutation callbacks (in component, not in hook):**
       - `onSuccess`: `enqueueSnackbar('Conta criada!', { variant: 'success' })` then `navigate('/', { replace: true })`
       - `onError`: Parse error response:
         - If status 409 or message contains "CPF"/"cpf": `setError('cpf', { message: error.response?.data?.message || 'CPF ja cadastrado' })`
         - Otherwise: `enqueueSnackbar('Erro inesperado. Tente novamente.', { variant: 'error' })`

    7. **Layout (per user decisions):**
       - `<Container maxWidth="sm">` wrapping a `<Card>` with `<CardContent>`
       - Welcome message above form: `<Typography variant="h5">` "Bem-vindo!" and `<Typography variant="body1" color="text.secondary">` "Complete seu cadastro para continuar."
       - Fields stacked vertically in a `<Box sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>`
       - Full-width submit button: `<Button type="submit" variant="contained" fullWidth loading={mutation.isPending}>Criar Conta</Button>`
       - Center card vertically with appropriate padding/margin (sx mt: 8 or similar)

    8. Run all tests -- OnboardingPage tests should now pass GREEN.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All OnboardingPage tests pass GREEN. Form validates name (2+ words, 2+ chars each) and CPF (check-digit via cpf-cnpj-validator) on blur. CPF auto-masks. Submit calls POST /accounts with unmasked data, seeds cache, shows toast, redirects. Backend errors show inline field errors. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` -- all tests pass (cpf-utils + AccountGuard + OnboardingPage)
2. `npx tsc --noEmit` -- no TypeScript errors
3. `npx vite build` -- builds without errors
</verification>

<success_criteria>
- User can fill name and CPF on onboarding form and submit successfully
- Invalid CPF shows "CPF invalido" inline error on blur
- Invalid name shows appropriate inline error on blur
- CPF auto-masks as user types (XXX.XXX.XXX-XX)
- Successful submission seeds React Query cache and redirects to /
- Toast "Conta criada!" appears on success
- Backend CPF-duplicate error shows inline error on CPF field
- Submit button shows loading spinner during request
- All tests green, TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding/02-02-SUMMARY.md`
</output>
