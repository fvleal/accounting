---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/App.tsx
  - src/api/client.ts
  - src/auth/AuthProviderWithNavigate.tsx
  - src/auth/ProtectedRoute.tsx
  - src/components/layout/AppLayout.tsx
  - src/components/layout/Header.tsx
  - src/components/ui/LoadingScreen.tsx
  - src/pages/LoginPage.tsx
  - src/pages/HomePage.tsx
  - src/main.tsx
  - src/types/account.ts
  - src/__tests__/api-client.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - LAYO-01

must_haves:
  truths:
    - "Unauthenticated user sees a branded login page with a Login button"
    - "Clicking Login redirects to Auth0 Universal Login"
    - "After Auth0 authentication, user lands on the home page without a login loop"
    - "User can click avatar in header and see a dropdown with logout option"
    - "Clicking Logout redirects user back to login page"
    - "Layout is responsive: header adapts to mobile, content area is centered with max-width"
    - "Auth token persists across page refresh (refresh token rotation + localStorage)"
  artifacts:
    - path: "src/auth/AuthProviderWithNavigate.tsx"
      provides: "Auth0Provider wrapper with useNavigate for redirect callback"
      contains: "Auth0Provider"
    - path: "src/auth/ProtectedRoute.tsx"
      provides: "Route protection via withAuthenticationRequired"
      contains: "withAuthenticationRequired"
    - path: "src/api/client.ts"
      provides: "Axios instance with Bearer token interceptor"
      exports: ["default", "setTokenGetter"]
    - path: "src/components/layout/Header.tsx"
      provides: "AppBar with logo, avatar menu, logout"
      contains: "AppBar"
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Header + Outlet + responsive container"
      contains: "Outlet"
    - path: "src/pages/LoginPage.tsx"
      provides: "Branded landing page for unauthenticated users"
      contains: "loginWithRedirect"
    - path: "src/pages/HomePage.tsx"
      provides: "Placeholder authenticated page"
      min_lines: 10
  key_links:
    - from: "src/App.tsx"
      to: "src/auth/AuthProviderWithNavigate.tsx"
      via: "Provider wrapping routes"
      pattern: "AuthProviderWithNavigate"
    - from: "src/App.tsx"
      to: "src/auth/ProtectedRoute.tsx"
      via: "Protected route for home"
      pattern: "ProtectedRoute"
    - from: "src/components/layout/Header.tsx"
      to: "@auth0/auth0-react"
      via: "useAuth0 hook for user info and logout"
      pattern: "useAuth0"
    - from: "src/api/client.ts"
      to: "src/auth/AuthProviderWithNavigate.tsx"
      via: "setTokenGetter called with getAccessTokenSilently"
      pattern: "setTokenGetter"

user_setup:
  - service: auth0
    why: "Auth0 SPA application must exist for frontend login flow"
    env_vars:
      - name: VITE_AUTH0_DOMAIN
        source: "Auth0 Dashboard -> Applications -> Your App -> Settings -> Domain"
      - name: VITE_AUTH0_CLIENT_ID
        source: "Auth0 Dashboard -> Applications -> Your App -> Settings -> Client ID"
      - name: VITE_AUTH0_AUDIENCE
        source: "Auth0 Dashboard -> APIs -> Your API -> API Identifier"
    dashboard_config:
      - task: "Create or verify SPA application exists (Application Type: Single Page Application)"
        location: "Auth0 Dashboard -> Applications -> Create Application"
      - task: "Set Allowed Callback URLs to http://localhost:5173"
        location: "Auth0 Dashboard -> Applications -> Your App -> Settings"
      - task: "Set Allowed Logout URLs to http://localhost:5173"
        location: "Auth0 Dashboard -> Applications -> Your App -> Settings"
      - task: "Set Allowed Web Origins to http://localhost:5173"
        location: "Auth0 Dashboard -> Applications -> Your App -> Settings"
      - task: "Enable Refresh Token Rotation"
        location: "Auth0 Dashboard -> APIs -> Your API -> Settings"
      - task: "Enable RBAC and 'Add Permissions in the Access Token'"
        location: "Auth0 Dashboard -> APIs -> Your API -> Settings"
---

<objective>
Build the Auth0 login/logout flow, Axios API client with token injection, responsive layout shell (header + content), and all pages. After this plan, the app is a functional authenticated shell.

Purpose: This is the core integration layer. Auth0 + API client + layout must work together. The header avatar menu, logout flow, and protected routes are all wired in a single plan to avoid half-built auth states.

Output: Working authenticated SPA — login via Auth0, protected home page, header with logout, responsive layout.
</objective>

<execution_context>
@C:/Users/felip/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/felip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — executor needs these to build upon -->

From src/theme.ts:
```typescript
export const theme: Theme; // MUI createTheme with dark mode, cssVariables: true
```

From src/main.tsx:
```typescript
// Provider stack order (Auth0Provider will be added inside BrowserRouter):
// StrictMode > StyledEngineProvider > ThemeProvider > CssBaseline > BrowserRouter > QueryClientProvider > SnackbarProvider > App
```

From src/index.css:
```css
@layer theme, mui, components, utilities;
@import "tailwindcss";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Auth0 provider, protected route, API client, and loading screen</name>
  <files>src/auth/AuthProviderWithNavigate.tsx, src/auth/ProtectedRoute.tsx, src/api/client.ts, src/components/ui/LoadingScreen.tsx, src/types/account.ts</files>
  <action>
1. Create `src/auth/AuthProviderWithNavigate.tsx`:
   - Wraps children in Auth0Provider
   - Uses `useNavigate()` from react-router for `onRedirectCallback`
   - Reads env vars: `VITE_AUTH0_DOMAIN`, `VITE_AUTH0_CLIENT_ID`, `VITE_AUTH0_AUDIENCE`
   - Sets `useRefreshTokens: true` and `cacheLocation: 'localstorage'` (critical for Safari/Brave)
   - Sets `authorizationParams.scope: 'openid profile email'`
   - Sets `authorizationParams.redirect_uri: window.location.origin`
   - Calls `setTokenGetter(getAccessTokenSilently)` via useEffect to wire up the API client
   - Follow the exact pattern from 01-RESEARCH.md Pattern 2, but also add the setTokenGetter useEffect here (see Pattern: Token Getter Initialization in research)

2. Create `src/auth/ProtectedRoute.tsx`:
   - Uses `withAuthenticationRequired` HOC from @auth0/auth0-react
   - Shows `LoadingScreen` while redirecting via `onRedirecting` prop
   - Accepts a `component` prop (ComponentType)
   - Follow exact pattern from 01-RESEARCH.md "Protected Route Component"

3. Create `src/api/client.ts`:
   - Axios instance with baseURL from `VITE_API_URL` (default `http://localhost:3000`)
   - `setTokenGetter` export function to receive `getAccessTokenSilently` reference
   - Request interceptor: attaches `Bearer ${token}` header. On token fetch failure, request proceeds without header (Auth0 SDK handles re-login)
   - Response interceptor: on 401, call `window.location.href = '/'` to trigger re-auth (simple approach — Auth0 SDK will handle the rest). For all errors, reject the promise so React Query / callers can handle.
   - Follow exact pattern from 01-RESEARCH.md Pattern 4
   - IMPORTANT: Do NOT import `useAuth0` in this file — it is not a React component. The token getter is injected via `setTokenGetter`.

4. Create `src/components/ui/LoadingScreen.tsx`:
   - Full-screen centered spinner on dark background (#121212)
   - Uses MUI CircularProgress component
   - Used during Auth0 SDK initialization and redirect processing
   - Must look intentional (not a broken state) — add the app name or a subtle label

5. Create `src/types/account.ts`:
   - Define the Account interface matching the backend model (prepared for Phase 2):
     ```typescript
     export interface Account {
       id: string;
       fullName: string;
       email: string;
       cpf: string;
       dateOfBirth: string | null;
       phone: string | null;
       photoUrl: string | null;
       createdAt: string;
       updatedAt: string;
     }
     ```
  </action>
  <verify>
    <automated>cd C:/Users/felip/Desktop/accounting/frontend && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Auth0 provider wrapper, protected route HOC, Axios client with token interceptor, loading screen, and Account type all exist and the project builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create layout shell, pages, and wire routing</name>
  <files>src/components/layout/AppLayout.tsx, src/components/layout/Header.tsx, src/pages/LoginPage.tsx, src/pages/HomePage.tsx, src/App.tsx, src/main.tsx</files>
  <action>
1. Create `src/components/layout/Header.tsx`:
   - MUI AppBar (position="sticky") with Toolbar
   - Left side: Typography with app name "Minha Conta" (or "Account")
   - Right side: MUI IconButton with Avatar showing `user.picture` from `useAuth0()`. If no picture, show initials from `user.name`
   - Avatar click opens MUI Menu (dropdown) containing:
     - User's name (Typography, non-clickable)
     - User's email (Typography, smaller, non-clickable)
     - MUI Divider
     - "Sair" (Logout) MenuItem that calls `logout({ logoutParams: { returnTo: window.location.origin } })`
   - Responsive: on mobile (xs), app name can use smaller variant. Menu works the same on all sizes.
   - Use MUI components for everything structural. Tailwind only for minor spacing adjustments if needed.

2. Create `src/components/layout/AppLayout.tsx`:
   - Renders `<Header />` at the top
   - MUI Container with `maxWidth="sm"` (roughly 600px) below the header, centered
   - Inside Container, render `<Outlet />` from react-router for nested routes
   - This creates the "header + centered content" layout per user decision (matches Google Personal Info reference)

3. Create `src/pages/LoginPage.tsx`:
   - Branded landing page for unauthenticated users (NOT auto-redirect — user decision)
   - Centered content: app name/logo, brief description, prominent "Entrar" (Login) MUI Button
   - Button calls `loginWithRedirect()` from `useAuth0()`
   - Dark background, clean look. Use MUI Paper or Box for the card.
   - If `isAuthenticated` is already true, redirect to "/" using `Navigate` from react-router

4. Create `src/pages/HomePage.tsx`:
   - Simple placeholder page for authenticated users
   - MUI Typography: "Bem-vindo" (Welcome) + user name from `useAuth0().user.name`
   - Brief text: "Pagina de perfil em breve" (Profile page coming soon)
   - This page will be replaced by the profile page in Phase 3

5. Update `src/App.tsx` — Set up routing:
   - Wrap all routes in `<AuthProviderWithNavigate>` (must be INSIDE BrowserRouter, which is in main.tsx)
   - Routes:
     - `/login` → `<LoginPage />`
     - `/` → `<AppLayout />` (layout route) with nested:
       - index → `<ProtectedRoute component={HomePage} />`
   - Auth0Provider must be a child of BrowserRouter (already is via main.tsx > BrowserRouter > App > AuthProviderWithNavigate)

6. Update `src/main.tsx` — No structural changes needed from Plan 01, but verify the Auth0Provider placement is correct. The Auth0Provider lives in App.tsx (inside AuthProviderWithNavigate), NOT in main.tsx. This is because Auth0Provider needs access to `useNavigate()` which requires being inside BrowserRouter.
  </action>
  <verify>
    <automated>cd C:/Users/felip/Desktop/accounting/frontend && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Header with avatar menu and logout, responsive layout with centered content, login page with Login button, protected home page — all wired with react-router. Build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create API client test and run full test suite</name>
  <files>src/__tests__/api-client.test.ts</files>
  <behavior>
    - Test 1: Axios instance has correct baseURL (defaults to http://localhost:3000)
    - Test 2: After setTokenGetter is called, request interceptor attaches Authorization header with Bearer token
    - Test 3: Without setTokenGetter, request proceeds without Authorization header
  </behavior>
  <action>
Create `src/__tests__/api-client.test.ts`:
- Import `apiClient` (default export) and `setTokenGetter` from `../api/client`
- Test that `apiClient.defaults.baseURL` is `http://localhost:3000` (default when VITE_API_URL is not set)
- Test the interceptor by:
  1. Calling `setTokenGetter(() => Promise.resolve('test-token-123'))`
  2. Using axios-mock-adapter or manually intercepting to verify the Authorization header is set to `Bearer test-token-123`
  - Alternative simpler approach: mock the interceptor by checking `apiClient.interceptors.request` handlers exist (at least 1 interceptor registered)

Keep tests focused and fast. Do not test Auth0 SDK behavior (that is manual verification).

Run `npx vitest run` to confirm all tests (theme + api-client) pass.
  </action>
  <verify>
    <automated>cd C:/Users/felip/Desktop/accounting/frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>API client tests pass confirming token interceptor works, full test suite (theme + api-client) is green</done>
</task>

</tasks>

<verification>
1. `npx vite build` completes without errors
2. `npx vitest run` — all tests pass (theme + api-client)
3. Manual: `npx vite --host` — visit localhost:5173
   - See login page with "Entrar" button (not auto-redirect)
   - Click Login → Auth0 Universal Login appears
   - Complete login → redirected to home page, no login loop
   - Header shows avatar menu → click → see name, email, "Sair"
   - Click "Sair" → redirected back to login page
   - Refresh page while logged in → stays logged in (refresh token rotation)
   - Resize browser → layout adapts (header responsive, content centered)
</verification>

<success_criteria>
- Auth0 redirect login works without login loop in React strict mode
- Logout from header menu redirects to login page
- Access token is JWT (not opaque) with correct audience and permissions claims
- Token persists across page refresh in Safari and Brave
- Header has app name on left, avatar menu on right
- Content area centered with max-width ~600px
- Layout responsive across breakpoints
- Axios client injects Bearer token on API requests
- All automated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
