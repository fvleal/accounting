---
phase: 04-profile-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/validation.ts
  - src/__tests__/validation.test.ts
  - src/hooks/useUpdateAccount.ts
  - src/components/profile/ProfileFieldRow.tsx
  - src/components/profile/EditNameModal.tsx
  - src/__tests__/EditNameModal.test.tsx
  - src/pages/OnboardingPage.tsx
autonomous: true
requirements: [EDIT-01, EDIT-04]

must_haves:
  truths:
    - "User can click the name row and see a modal with the current name pre-filled"
    - "User can edit the name, click Save, and the modal closes with a success toast"
    - "User sees validation errors if name is fewer than 2 words or any word < 2 characters"
    - "If the server rejects the update, the modal stays open and an error toast appears"
  artifacts:
    - path: "src/utils/validation.ts"
      provides: "Shared nameRules extracted from OnboardingPage"
      exports: ["nameRules"]
    - path: "src/hooks/useUpdateAccount.ts"
      provides: "PATCH mutation hook mirroring useCreateAccount"
      exports: ["useUpdateAccount"]
    - path: "src/components/profile/EditNameModal.tsx"
      provides: "Name edit dialog with react-hook-form validation"
      exports: ["EditNameModal"]
    - path: "src/components/profile/ProfileFieldRow.tsx"
      provides: "Updated row with onClick prop and pointer cursor"
      exports: ["ProfileFieldRow"]
  key_links:
    - from: "src/components/profile/EditNameModal.tsx"
      to: "src/hooks/useUpdateAccount.ts"
      via: "useUpdateAccount() mutation call on form submit"
      pattern: "useUpdateAccount"
    - from: "src/components/profile/EditNameModal.tsx"
      to: "src/utils/validation.ts"
      via: "nameRules import for Controller rules"
      pattern: "import.*nameRules.*from.*validation"
    - from: "src/hooks/useUpdateAccount.ts"
      to: "src/api/accounts.ts"
      via: "updateAccount API call"
      pattern: "updateAccount"
    - from: "src/hooks/useUpdateAccount.ts"
      to: "React Query cache"
      via: "setQueryData(['account', 'me'], account) on success"
      pattern: "setQueryData.*account.*me"
---

<objective>
Create the shared editing infrastructure (validation rules, update mutation hook, clickable field rows) and the EditNameModal component with full test coverage.

Purpose: Establishes the reusable foundation for all profile editing and delivers the name editing feature (EDIT-01). The mutation hook and validation utilities are shared by both name and birthday editing.
Output: Clickable ProfileFieldRow, shared validation.ts, useUpdateAccount hook, EditNameModal with tests.
</objective>

<execution_context>
@C:/Users/felip/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/felip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-profile-editing/04-CONTEXT.md
@.planning/phases/04-profile-editing/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/account.ts:
```typescript
export interface Account {
  id: string;
  name: string;
  email: string;
  cpf: string;
  birthDate: string | null;
  phone: string | null;
  phoneVerified: boolean;
  photoUrl: string | null;
  createdAt: string;
  updatedAt: string;
}
```

From src/api/accounts.ts:
```typescript
export function updateAccount(id: string, data: { name?: string; birthDate?: string }): Promise<Account>;
```

From src/hooks/useCreateAccount.ts (pattern to mirror):
```typescript
export function useCreateAccount() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: { name: string; cpf: string }) => createAccount(data),
    onSuccess: (account: Account) => {
      queryClient.setQueryData(['account', 'me'], account);
    },
  });
}
```

From src/pages/OnboardingPage.tsx (nameRules to extract, lines 22-31):
```typescript
const nameRules = {
  required: 'Nome completo e obrigatorio',
  validate: (value: string) => {
    const words = value.trim().split(/\s+/);
    if (words.length < 2) return 'Informe nome e sobrenome';
    if (words.some((w) => w.length < 2))
      return 'Cada parte do nome deve ter pelo menos 2 caracteres';
    return true;
  },
};
```

From src/components/profile/ProfileFieldRow.tsx:
```typescript
interface ProfileFieldRowProps {
  label: string;
  value: string | null;
  editable?: boolean;
}
export function ProfileFieldRow({ label, value, editable }: ProfileFieldRowProps) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared validation and create useUpdateAccount hook</name>
  <files>src/utils/validation.ts, src/__tests__/validation.test.ts, src/hooks/useUpdateAccount.ts, src/pages/OnboardingPage.tsx</files>
  <behavior>
    - nameRules.required returns 'Nome completo e obrigatorio' for empty string
    - nameRules.validate returns 'Informe nome e sobrenome' for single word
    - nameRules.validate returns 'Cada parte do nome deve ter pelo menos 2 caracteres' for words < 2 chars
    - nameRules.validate returns true for 'Felipe Vieira'
    - nameRules.validate returns true for '  Felipe   Vieira  ' (handles extra whitespace)
  </behavior>
  <action>
    1. Create `src/utils/validation.ts` exporting `nameRules` object with the exact same logic currently in OnboardingPage.tsx lines 22-31.

    2. Create `src/__tests__/validation.test.ts` with unit tests for nameRules covering: empty string, single word, word with 1 char, valid 2+ word name, extra whitespace trimming.

    3. Create `src/hooks/useUpdateAccount.ts` mirroring useCreateAccount exactly:
       - `useMutation` with `mutationFn` calling `updateAccount(id, data)`
       - `onSuccess` calls `queryClient.setQueryData(['account', 'me'], account)`
       - Input type: `{ id: string; data: { name?: string; birthDate?: string } }`
       - Return type matches useMutation return

    4. Update `src/pages/OnboardingPage.tsx`:
       - Replace local `nameRules` with `import { nameRules } from '../utils/validation'`
       - Delete the local nameRules const (lines 22-31)
       - Everything else stays identical
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/validation.test.ts src/__tests__/OnboardingPage.test.tsx --reporter=verbose</automated>
  </verify>
  <done>validation.ts exports nameRules, OnboardingPage imports from it (no local copy), useUpdateAccount hook exists, all validation tests pass, OnboardingPage tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Add onClick to ProfileFieldRow and create EditNameModal</name>
  <files>src/components/profile/ProfileFieldRow.tsx, src/components/profile/EditNameModal.tsx</files>
  <action>
    1. Update `src/components/profile/ProfileFieldRow.tsx`:
       - Add `onClick?: () => void` to ProfileFieldRowProps interface
       - Add `onClick` to destructured props
       - On the outer Box: add `onClick={editable ? onClick : undefined}` and `sx={{ cursor: editable ? 'pointer' : 'default' }}` (merge with existing sx)
       - Add `role="button"` and `tabIndex={0}` when editable for accessibility

    2. Create `src/components/profile/EditNameModal.tsx`:
       - Props: `{ open: boolean; onClose: () => void; account: Account }`
       - Conditionally render: `if (!open) return null;` (prevents stale defaultValues -- see Pitfall 2 in RESEARCH.md)
       - Use `useForm<{ name: string }>({ defaultValues: { name: account.name }, mode: 'onBlur' })`
       - Controller for name field with `rules={nameRules}` from `src/utils/validation.ts`
       - MUI Dialog with `disableEscapeKeyDown` and `onClose={(_e, reason) => { if (reason === 'backdropClick') return; }}` per user decision
       - DialogTitle: "Editar nome"
       - DialogContent: TextField for name, full width, label "Nome completo"
       - DialogActions: Cancel button and Save button
       - Both Cancel and Save disabled while `mutation.isPending` (use `useUpdateAccount()`)
       - Cancel onClick calls `onClose()`
       - Save onClick calls `handleSubmit(onSubmit)` where onSubmit:
         - Calls `mutation.mutate({ id: account.id, data: { name: values.name.trim() } }, { onSuccess: () => { onClose(); enqueueSnackbar('Nome atualizado!', { variant: 'success' }); }, onError: (error: any) => { const msg = error?.response?.data?.message || 'Erro ao atualizar nome.'; enqueueSnackbar(msg, { variant: 'error' }); } })`
       - Save button uses MUI v7 `loading={mutation.isPending}` prop
       - All labels in Brazilian Portuguese per CONTEXT.md
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>ProfileFieldRow accepts onClick and shows pointer cursor when editable. EditNameModal renders Dialog with name field, validates on blur, calls PATCH on save, shows success/error toasts, prevents backdrop/escape close.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create EditNameModal integration tests</name>
  <files>src/__tests__/EditNameModal.test.tsx</files>
  <behavior>
    - Modal renders with current name pre-filled when open=true
    - Modal does not render when open=false
    - Cancel button calls onClose
    - Save button is disabled while mutation is pending
    - Submitting valid name calls updateAccount with correct payload
    - Submitting invalid name (single word) shows validation error, does not call API
    - On success: onClose is called, success toast 'Nome atualizado!' appears
    - On error: onClose is NOT called, error toast appears, modal stays open
  </behavior>
  <action>
    Create `src/__tests__/EditNameModal.test.tsx` following the established test pattern from ProfilePage.test.tsx:
    - Mock `useUpdateAccount` returning `{ mutate: mockMutate, isPending: false }` (and a pending variant)
    - Mock `notistack` useSnackbar
    - Wrap in QueryClientProvider + SnackbarProvider + MemoryRouter
    - Test: renders nothing when open=false
    - Test: shows "Editar nome" title and pre-filled name when open=true
    - Test: Cancel button calls onClose
    - Test: typing invalid name (single word) and blurring shows validation error
    - Test: submitting valid name calls mutate with `{ id: account.id, data: { name: 'New Name' } }`
    - Test: onSuccess callback calls onClose and enqueueSnackbar with 'Nome atualizado!'
    - Test: onError callback does NOT call onClose, calls enqueueSnackbar with error variant
    - Test: Save and Cancel buttons disabled when isPending=true

    Use `@testing-library/react` userEvent for interactions. Use `waitFor` for async assertions.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/EditNameModal.test.tsx --reporter=verbose</automated>
  </verify>
  <done>All EditNameModal tests pass covering EDIT-01 (name editing) and EDIT-04 (error handling with rollback) scenarios.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```bash
npx vitest run --reporter=verbose
```
All existing tests (OnboardingPage, ProfilePage, AccountGuard) must still pass alongside new validation and EditNameModal tests.
</verification>

<success_criteria>
- nameRules extracted to shared validation.ts with unit tests passing
- OnboardingPage uses imported nameRules (no local duplicate)
- useUpdateAccount hook exists mirroring useCreateAccount pattern
- ProfileFieldRow accepts onClick prop with pointer cursor on editable rows
- EditNameModal opens with pre-filled name, validates on blur, calls PATCH, shows toasts
- Modal prevents backdrop click and Escape key dismissal
- All tests (new + existing) pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-profile-editing/04-01-SUMMARY.md`
</output>
