---
phase: 03-profile-display
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/pages/ProfilePage.tsx
  - src/pages/HomePage.tsx
  - src/components/profile/ProfileHero.tsx
  - src/components/profile/ProfileSectionCard.tsx
  - src/components/profile/ProfileFieldRow.tsx
  - src/components/profile/ProfileSkeleton.tsx
  - src/App.tsx
  - src/__tests__/ProfilePage.test.tsx
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04

must_haves:
  truths:
    - "User sees all profile fields (name, email, CPF, birthday, phone) in read-only cards"
    - "User sees skeleton placeholders while data is loading, no blank page flash"
    - "User without a photo sees an initials-based avatar with colored background"
    - "User sees a toast notification on profile fetch error"
    - "Empty fields (birthday, phone) display 'Nao informado' in muted text"
    - "Editable rows (name, birthday, phone) show chevron icon; immutable rows (email, CPF) do not"
  artifacts:
    - path: "src/pages/ProfilePage.tsx"
      provides: "Main profile page replacing HomePage"
      min_lines: 20
    - path: "src/components/profile/ProfileHero.tsx"
      provides: "Avatar + name + email hero section"
      min_lines: 15
    - path: "src/components/profile/ProfileSectionCard.tsx"
      provides: "Reusable card wrapper with title and dividers"
      min_lines: 10
    - path: "src/components/profile/ProfileFieldRow.tsx"
      provides: "Label/value row with optional chevron"
      min_lines: 10
    - path: "src/components/profile/ProfileSkeleton.tsx"
      provides: "Full-page skeleton matching layout structure"
      min_lines: 20
    - path: "src/__tests__/ProfilePage.test.tsx"
      provides: "Tests for PROF-01 through PROF-04"
      min_lines: 50
  key_links:
    - from: "src/pages/ProfilePage.tsx"
      to: "src/hooks/useAccount.ts"
      via: "useAccount() hook call"
      pattern: "useAccount\\(\\)"
    - from: "src/pages/ProfilePage.tsx"
      to: "src/components/profile/ProfileSkeleton.tsx"
      via: "conditional render on isLoading"
      pattern: "isLoading.*ProfileSkeleton"
    - from: "src/pages/ProfilePage.tsx"
      to: "notistack"
      via: "enqueueSnackbar on error"
      pattern: "enqueueSnackbar.*error"
    - from: "src/components/profile/ProfileHero.tsx"
      to: "src/utils/initials.ts"
      via: "getInitials and getAvatarColor imports"
      pattern: "import.*from.*utils/initials"
    - from: "src/App.tsx"
      to: "src/pages/ProfilePage.tsx"
      via: "Route element replacing HomePage"
      pattern: "ProfilePage"
---

<objective>
Build the complete read-only profile page with hero section, section cards, field rows, skeleton loading, and toast notifications. Replace the placeholder HomePage with ProfilePage.

Purpose: This is the core deliverable of Phase 3 -- users can view all their personal information in a Google Personal Info-style layout with proper loading states and error feedback.
Output: ProfilePage with all subcomponents, full test coverage, wired into routing.
</objective>

<execution_context>
@C:/Users/felip/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/felip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-profile-display/03-CONTEXT.md
@.planning/phases/03-profile-display/03-RESEARCH.md
@.planning/phases/03-profile-display/03-01-SUMMARY.md

@src/types/account.ts
@src/hooks/useAccount.ts
@src/App.tsx
@src/pages/HomePage.tsx
@src/pages/OnboardingPage.tsx
@src/__tests__/OnboardingPage.test.tsx

<interfaces>
<!-- From Plan 01 outputs -->
From src/utils/initials.ts:
```typescript
export function getInitials(fullName: string): string;
export function getAvatarColor(name: string): string;
```

From src/utils/phone.ts:
```typescript
export function formatPhone(phone: string | null): string | null;
```

From src/utils/date.ts:
```typescript
export function formatBirthday(dateStr: string | null): string | null;
```

From src/utils/cpf.ts:
```typescript
export function maskCpf(value: string): string;
```

<!-- Data hook -->
From src/hooks/useAccount.ts:
```typescript
export function useAccount(): UseQueryResult<Account>;
// Returns { data, isLoading, error, isError }
```

<!-- Account type -->
From src/types/account.ts:
```typescript
export interface Account {
  id: string;
  fullName: string;
  email: string;
  cpf: string;
  dateOfBirth: string | null;
  phone: string | null;
  photoUrl: string | null;
  createdAt: string;
  updatedAt: string;
}
```

<!-- Routing structure from App.tsx -->
App.tsx routing: HomePage is rendered at `<Route index element={<HomePage />} />` inside AccountGuard > AppLayout.
Replace HomePage import with ProfilePage import. Change element to `<ProfilePage />`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build profile components and ProfilePage, wire into routing</name>
  <files>src/components/profile/ProfileHero.tsx, src/components/profile/ProfileSectionCard.tsx, src/components/profile/ProfileFieldRow.tsx, src/components/profile/ProfileSkeleton.tsx, src/pages/ProfilePage.tsx, src/App.tsx, src/pages/HomePage.tsx</files>
  <action>
    Build all profile components following the patterns in RESEARCH.md and locked decisions in CONTEXT.md.

    1. **Create `src/components/profile/ProfileFieldRow.tsx`:**
       - Props: `{ label: string; value: string | null; editable?: boolean }`
       - Layout: flex row, label on left (Typography variant="body2" color="text.secondary"), value on right (Typography variant="body2", color="text.primary" if value exists, "text.secondary" if null).
       - When value is null/undefined, display "Nao informado" in text.secondary color.
       - When `editable` is true, render ChevronRight icon (from @mui/icons-material) after the value, sx={{ fontSize: 20, color: 'text.secondary' }}. No onClick handler (Phase 4 concern).
       - Use Box with sx: `display: 'flex', alignItems: 'center', justifyContent: 'space-between', py: 1.5, px: 2`.

    2. **Create `src/components/profile/ProfileSectionCard.tsx`:**
       - Props: `{ title: string; children: React.ReactNode }`
       - MUI Card with CardContent. Title as Typography variant="subtitle2" color="text.secondary" with mb: 1.
       - Render children inside. Add MUI Divider between each child using React.Children.toArray and interleaving Dividers.
       - Card sx: mb: 2 for spacing between cards.

    3. **Create `src/components/profile/ProfileHero.tsx`:**
       - Props: `{ account: Account }` (import Account type)
       - Layout: centered flex column with mb: 3, mt: 2.
       - MUI Avatar: width 80, height 80, src={account.photoUrl ?? undefined} (CRITICAL: use ?? undefined, NOT empty string -- see RESEARCH.md Pitfall 1).
       - Avatar children: getInitials(account.fullName) from src/utils/initials.ts.
       - Avatar bgcolor: getAvatarColor(account.fullName) from src/utils/initials.ts.
       - Avatar fontSize: '1.5rem'.
       - Below avatar: Typography variant="h6" for fullName, Typography variant="body2" color="text.secondary" for email.

    4. **Create `src/components/profile/ProfileSkeleton.tsx`:**
       - No props. Renders skeleton matching exact layout structure.
       - Hero area: Skeleton variant="circular" width={80} height={80}, then Skeleton variant="text" width={160} (name), then Skeleton variant="text" width={200} (email).
       - Card 1 (3 rows): Card > CardContent > Skeleton variant="text" width={180} for title + 3x Skeleton variant="text" height={48} for rows.
       - Card 2 (2 rows): Same pattern with Skeleton variant="text" width={200} for title + 2x Skeleton rows.
       - Must use same Card/CardContent structure and spacing as real content to prevent layout shift.

    5. **Create `src/pages/ProfilePage.tsx`:**
       - Use useAccount() hook to get { data: account, isLoading, isError, error }.
       - Error toast: useEffect that fires enqueueSnackbar('Erro ao carregar perfil.', { variant: 'error' }) when isError transitions to true. Track with a ref or check `isError && !isFetching` to avoid stale-error-on-mount issue (RESEARCH.md Pitfall 5). Use `useSnackbar()` from notistack.
       - If isLoading, render `<ProfileSkeleton />`.
       - If !account (after loading), return null (AccountGuard guarantees account exists).
       - Otherwise render:
         - `<ProfileHero account={account} />`
         - `<ProfileSectionCard title="Informacoes basicas">` with 3 ProfileFieldRow:
           - label="Nome", value={account.fullName}, editable
           - label="Email", value={account.email} (NOT editable)
           - label="CPF", value={maskCpf(account.cpf)} (NOT editable)
         - `<ProfileSectionCard title="Informacoes adicionais">` with 2 ProfileFieldRow:
           - label="Nascimento", value={formatBirthday(account.dateOfBirth)}, editable
           - label="Telefone", value={formatPhone(account.phone)}, editable

    6. **Update `src/App.tsx`:**
       - Replace `import { HomePage } from './pages/HomePage';` with `import { ProfilePage } from './pages/ProfilePage';`
       - Replace `<Route index element={<HomePage />} />` with `<Route index element={<ProfilePage />} />`

    7. **Delete or clear `src/pages/HomePage.tsx`:** Since it is still tracked in git and referenced nowhere after App.tsx update, keep the file but it will no longer be imported. Alternatively, delete it. Preference: delete it to keep the project clean.

    8. Verify the app compiles: `npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd C:/Users/felip/Desktop/accounting/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>All 4 profile components created. ProfilePage wired into App.tsx routing. TypeScript compiles with no errors. HomePage removed.</done>
</task>

<task type="auto">
  <name>Task 2: Write ProfilePage integration tests</name>
  <files>src/__tests__/ProfilePage.test.tsx</files>
  <action>
    Create comprehensive tests for ProfilePage covering all four PROF requirements.

    **Test setup pattern** (follow OnboardingPage.test.tsx pattern):
    - Mock useAccount hook: `vi.mock('../hooks/useAccount')` returning controlled states.
    - Mock notistack useSnackbar: same pattern as OnboardingPage tests.
    - Wrapper: QueryClientProvider + MemoryRouter + SnackbarProvider.

    **Test cases:**

    PROF-01 - Profile fields displayed:
    - "displays all profile fields with formatted values" -- mock useAccount returning full account data ({ fullName: 'Felipe Vieira', email: 'felipe@test.com', cpf: '52998224725', dateOfBirth: '2000-01-15', phone: '11987654321', photoUrl: null }). Assert:
      - screen.getByText('Felipe Vieira') visible (hero name)
      - screen.getByText('felipe@test.com') visible (hero email + field)
      - screen.getByText('529.982.247-25') visible (formatted CPF)
      - screen.getByText('15/01/2000') visible (formatted birthday)
      - screen.getByText('(11) 98765-4321') visible (formatted phone)
    - "displays 'Nao informado' for null fields" -- mock account with dateOfBirth: null, phone: null. Assert two instances of 'Nao informado' text.
    - "shows chevron for editable rows only" -- check that Nome, Nascimento, Telefone rows have ChevronRight (use testId or aria), Email and CPF rows do not. Approach: the ChevronRight icon can be detected by its SVG data-testid if added, or by querying within specific row containers.

    PROF-02 - Skeleton loading:
    - "shows skeleton while loading" -- mock useAccount with isLoading: true, data: undefined. Assert that MUI Skeleton elements are rendered (role="progressbar" or MuiSkeleton class). Assert that real field values are NOT visible.

    PROF-03 - Toast on error:
    - "shows error toast on fetch failure" -- mock useAccount with isError: true, error: new Error('fail'). Assert enqueueSnackbar was called with 'Erro ao carregar perfil.' and variant: 'error'.

    PROF-04 - Initials avatar:
    - "shows initials avatar when no photo" -- mock account with photoUrl: null, fullName: 'Felipe Vieira'. Assert Avatar renders text "FV". Can query by text content within the avatar area.
    - "shows photo avatar when photoUrl exists" -- mock account with photoUrl: 'https://example.com/photo.jpg'. Assert an img element with that src exists.

    Run all tests to confirm green.
  </action>
  <verify>
    <automated>cd C:/Users/felip/Desktop/accounting/frontend && npx vitest run src/__tests__/ProfilePage.test.tsx --reporter=verbose</automated>
  </verify>
  <done>ProfilePage.test.tsx covers PROF-01 (field display, null fields, chevron), PROF-02 (skeleton), PROF-03 (error toast), PROF-04 (initials avatar, photo avatar). All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- full suite green (existing + new tests)
- `npx tsc --noEmit` -- no type errors
- ProfilePage renders at "/" route inside AccountGuard
- All four PROF requirements have test coverage
</verification>

<success_criteria>
- ProfilePage replaces HomePage at "/" route
- Hero section shows avatar (photo or initials) + name + email
- "Informacoes basicas" card shows Nome (editable), Email, CPF rows
- "Informacoes adicionais" card shows Nascimento (editable), Telefone (editable) rows
- Skeleton renders during loading with matching layout structure
- Error toast fires on fetch failure
- Null fields show "Nao informado"
- Editable rows show chevron, immutable rows do not
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-profile-display/03-02-SUMMARY.md`
</output>
